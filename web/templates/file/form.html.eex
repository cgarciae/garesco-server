<%= form_for @changeset, @action, fn f -> %>
  <%= if @changeset.action do %>
    <div class="alert alert-danger">
      <p>Oops, something went wrong! Please check the errors below:</p>
      <ul>
        <%= for {attr, message} <- f.errors do %>
          <li><%= humanize(attr) %> <%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= label f, :filename, "Filename", class: "control-label" %>
    <%= text_input f, :filename, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= label f, :content_type, "Content type", class: "control-label" %>
    <%= text_input f, :content_type, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= label f, :version, "Version", class: "control-label" %>
    <%= number_input f, :version, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= submit "Submit", class: "btn btn-primary" %>
  </div>
<% end %>
